import { RefObject } from "react";

function initResizerFn(resizer: RefObject<HTMLDivElement>, sidebar: RefObject<HTMLDivElement>) {
    // track current mouse position in x var
    
}

export default initResizerFn;
